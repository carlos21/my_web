<!-- ARTICLES HEADER -->
<div class="articles-header">

  <!-- IMAGE -->
  <div class="image">
    <%= image_tag @article.image_url, :size => '50x50' %>
  </div>

  <div class="description">

    <!-- TITLE -->
    <div class="description-title"><%= @article.name %></div>

    <!-- INTRO -->
    <div class="description-intro"><%= @article.small_description %></div>

  </div>
  <% path = ('demos'  + '/' + @article.path) if @category.id == Constants.CATEGORY_DEMO_ID %>
  <% path = ('guides' + '/' + @article.path + @category.path) if @category.id != Constants.CATEGORY_DEMO_ID %>

  <div class="right-buttons">
    <%= render :partial => 'socialnetworks/facebook_button', :locals => {:url => root_url + path} %>
    <%= render :partial => 'socialnetworks/twitter_button', :locals => {:url => root_url + path} %>
    <% #render :partial => 'socialnetworks/googleplus_button', :locals => {:url => 'http://carlos21.com'} %>
  </div>

</div>

<!-- CONTENT -->
<div class="article-content">
  <%= markdown @article.content %>
</div>

<div class="comments">
  
  <!-- COMMENTS LIST -->
  <%= render partial: 'comment/list', locals: {comments: @article.comments} %>
  
  <!-- COMMENT FORM -->
  <%= render partial: 'comment/form' %>
  
  <div style="height:40px;"></div>
</div>


