<div class="articles-header">
  <div class="image">
    <%= image_tag @category_selected.image_url, :size => '50x50' %>
  </div>
  <div class="description">
    <div class="description-title"><%= @category_selected.name %></div>
    <div class="description-intro"><%= @category_selected.small_description %></div>
  </div>
</div>

<% @articles.each do |a| %>
  <div class="article">
    <div class="icon">
      <i class="icon-leaf"></i>
    </div>
    <%= link_to guides_category_and_article_path(@locale, @category_selected.path, a.path), :style =>'text-decoration: none;' do %>
      <div class="name"><%= a.name %></div>
      <div class="extra-info">
        <div><%= t(:messages_label)%> <%= a.messages %></div>
        <div><%= t(:visits_label)%> <%= a.visits %></div>
        <div><%= t(:last_update_label)%> <%= a.updated_at.to_s(:long) %></div>
      </div>
    <%  end %>
  </div>
<% end %>

  