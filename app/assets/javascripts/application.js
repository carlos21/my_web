// This is a manifest file that'll be compiled into application.js, which will include all the files
// listed below.
//
// Any JavaScript/Coffee file within this directory, lib/assets/javascripts, vendor/assets/javascripts,
// or vendor/assets/javascripts of plugins, if any, can be referenced here using a relative path.
//
// It's not advisable to add code directly here, but if you do, it'll appear at the bottom of the
// the compiled file.
//
// WARNING: THE FIRST BLANK LINE MARKS THE END OF WHAT'S TO BE PROCESSED, ANY BLANK LINE SHOULD
// GO AFTER THE REQUIRES BELOW.
//
// require jquery
// require jquery_ujs
//= require chat.events
//= require chat.functions
//= require navigation
//= require dashboard
//= require facebook.api
//= require header


/*function getDocWidth() {
  var D = document;
  return Math.max(
      Math.max(D.body.scrollWidth, D.documentElement.scrollWidth),
      Math.max(D.body.offsetWidth, D.documentElement.offsetWidth),
      Math.max(D.body.clientWidth, D.documentElement.clientWidth)
  );
}

var width = getDocWidth();
var nav_width = parseInt($('.navigation').css('width'));
var menu_width = parseInt($('.rightmenu').css('width'));
var caca = (width - nav_width - menu_width - 30);

if (nav_width >= 199 && menu_width >= 199) {
  console.log('entro webon');
  $('.content').width(caca);
  $('.section-content').width(caca);
}
*/
